You are a senior project manager.

Generate exactly 5 software development tasks.

Return ONLY valid JSON.

Example:

[
    {
        "title":"Build ETL logging"
    },
    {
        "title":"Optimize database indexing"
    }
]